name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
<<<<<<< HEAD
          cache-dependency-path: site/package-lock.json
          
      - name: Install dependencies
        run: |
          cd site
          npm ci
          
      - name: Build site
        run: |
          cd site
          npm run build
          
      - name: Prepare deployment directory
        run: |
          mkdir -p deploy
          cp -r site/dist/* deploy/
          cp -r quizzes deploy/
          
          # Create a custom 404 page for SPA routing support on GitHub Pages
          # This will redirect unknown routes to index.html where React Router can handle them
          cp site/dist/index.html deploy/404.html
=======
          cache-dependency-path: 'site/package-lock.json'
          
      - name: Install dependencies
        working-directory: ./site
        run: npm ci
          
      - name: Build Vite application
        working-directory: ./site
        run: npm run build
>>>>>>> 36b37b2db629ea452b0386b18ac558026e296681
          
      - name: Setup Pages
        uses: actions/configure-pages@v4
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
<<<<<<< HEAD
          path: './deploy'
=======
          path: './site/dist'
>>>>>>> 36b37b2db629ea452b0386b18ac558026e296681

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
<<<<<<< HEAD
        uses: actions/deploy-pages@v4
=======
        uses: actions/deploy-pages@v4 
>>>>>>> 36b37b2db629ea452b0386b18ac558026e296681
